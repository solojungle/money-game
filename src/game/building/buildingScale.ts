/** @deprecated Building scale multiplier removed — sizes match recipe ghostSize. */
export const BUILDING_WORLD_SCALE = 1;

export function scaleSize(
  size: [number, number, number],
): [number, number, number] {
  return size;
}
